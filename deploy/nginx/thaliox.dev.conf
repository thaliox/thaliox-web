server {
    listen 80;
    server_name thaliox.dev www.thaliox.dev;
    root /var/www/thaliox-dev;
    index index.html;
    location /_nuxt/ { expires 1y; add_header Cache-Control "public, immutable"; }
    location / { try_files $uri $uri/index.html /index.html; }
}
